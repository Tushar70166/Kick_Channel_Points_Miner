# 🟢 Kick Channel Points Miner

[![Python](https://img.shields.io/badge/Python-3.10%2B-blue)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green)](LICENSE)

> [🇺🇸 **Read in English**](README.md)

Мощный асинхронный бот для автоматического фарминга очков канала (Channel Points) на **Kick.com**. Включает современную веб-панель, продвинутое управление через Telegram и обход защиты Cloudflare.

---

## ✨ Возможности

*   **⚡ Мульти-фарминг:** Одновременная работа с множеством каналов.
*   **🛡️ Обход Cloudflare:** Встроенные механизмы для поддержки соединения.
*   **🖥️ Веб-дашборд:** Красивая панель управления в браузере (Flask).
*   **📱 Telegram Бот:**
    *   **Система прав:** Владелец имеет полный доступ, гости — только просмотр.
    *   **Уведомления:** Отчеты о начислении баллов и ошибках.
    *   **Удаленное управление:** Полный перезапуск майнера через Telegram.
*   **🌐 Мультиязычность:** Поддержка русского и английского языков.
*   **📉 Умное логирование:** Чистый вывод в консоль с возможностью включить Debug.

---

## 🚀 Установка

1.  **Скачайте** репозиторий.
2.  **Установите зависимости**:
    ```bash
    pip install -r requirements.txt
    ```
    *(Убедитесь, что установлены `playwright`, `curl_cffi`, `aiogram`, `flask`, `loguru`)*.

3.  **Настройка**: Переименуйте `config.example.json` в `config.json` и заполните его.

---

## ⚙️ Конфигурация (`config.json`)

Пример полного файла конфигурации:

```json
{
  "Language": "en",
  "Debug": false,
  "WebDashboard": {
    "enabled": true,
    "port": 5000
  },
  "Telegram": {
    "enabled": true,
    "bot_token": "YOUR_TELEGRAM_BOT_TOKEN",
    "chat_id": "YOUR_TELEGRAM_USER_ID",
    "allowed_users": [
        123456789
    ]
  },
  "Private": {
    "token": "YOUR_KICK_TOKEN_OR_COOKIE_STRING"
  },
  "Streamers": [
    "stream1",
    "stream2",
    "stream3"
  ]
}
```

### 🔑 Как получить токен Kick

1.  Зайдите на **Kick.com** и авторизуйтесь.
2.  Нажмите `F12`, чтобы открыть инструменты разработчика.
3.  Перейдите на вкладку **Network** (Сеть).
4.  Обновите страницу (`F5`).
5.  Нажмите на любой появившийся запрос (например, `auth`).
6.  В правой панели выберите вкладку **Headers** (Заголовки) и найдите раздел **Request Headers**.
7.  Найдите строку `Authorization`.
8.  Скопируйте длинную строку, которая идет **после** слова `Bearer`. Она выглядит вот так `123456789|*************************************`.
9. Вставьте эту строку в `config.json` в поле `"token"`.


### Описание параметров:

*   **`Language`**: `ru` или `en`.
*   **`Debug`**: `true` для подробных логов, `false` для чистого вывода.
*   **`WebDashboard`**:
    *   `enabled`: Включить веб-панель (`true`/`false`).
    *   `port`: Порт доступа (по умолчанию `http://localhost:5000`).
*   **`Telegram`**:
    *   `bot_token`: Токен бота.
    *   `chat_id`: Ваш ID (вы будете **Владельцем** с полными правами).
    *   `allowed_users`: Список ID пользователей, которые могут смотреть статус (Гости).
*   **`Private`**:
    *   `token`: Ваш токен авторизации Kick (можно найти в Cookies браузера).
*   **`Streamers`**: Список имен каналов (slug) для фарма.

---

## 🎮 Использование

Запуск майнера:
```bash
python main.py
```

### 📱 Команды Telegram

| Команда | Описание | Доступ |
| :--- | :--- | :--- |
| `/start` | Запуск бота и клавиатуры | Все |
| `/status` | Статус стримеров и аптайм | Все |
| `/balance` | Баланс по всем каналам | Все |
| `/help` | Список команд | Все |
| `/restart` | **Полный перезапуск майнера** | **Только Владелец** |
| `/language` | Смена языка бота (`ru`/`en`) | **Только Владелец** |

---

## 🖥️ Веб-дашборд

Если включено в конфиге, откройте **`http://localhost:5000`** в браузере.
Вы увидите таблицу в реальном времени:
*   Активные стримеры
*   Текущий баланс
*   Время последнего обновления
*   Статус соединения

---

## ⚠️ Отказ от ответственности

Это программное обеспечение создано исключительно в образовательных целях. Используйте его на свой страх и риск. Разработчик не несет ответственности за возможные блокировки аккаунтов на Kick.com.